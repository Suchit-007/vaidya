# Vaidya.ai Psychologically Driven Landing Page & Formatting Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Transform the Vaidya.ai landing page structure and content formatting into an authoritative, conversion-optimized interface powered by Mechanism-First persuasion psychology and premium visual formatting.

**Architecture:** The interface relies on persistent top-anchoring, high-agency proof density frames, and tactile input pre-configurations. Visual cues guide user attention systematically from domain credibility validation down to low-friction exploration.

**Tech Stack:** HTML5 Semantic Markup, CSS3 Variables (`styles.css`), Custom DOM Element Identifiers (`index.html`).

---

### Task 1: DOM Header & Hero Persuasion Formatting (`index.html`)

**Files:**
- Modify: `frontend/index.html:35-85`

- [ ] **Step 1: Format the Primary Domain Anchor**
  - Verify and apply explicit font weighting (`font-weight: 700`) and letter-spacing (`letter-spacing: 2px`) to the core title element (`<h1>Vaidya.ai</h1>`).
  - Anchor the sub-headline visually using the descriptive class string `class="subtitle"` to establish domain credibility instantly.

- [ ] **Step 2: Inject the Mechanism-First Sub-Headline**
  - Update the descriptive sub-headline text element to read exactly: "Clinical RAG Assistant powered by multi-textual TF-IDF matrix corroboration across classical Sanskrit archives and modern standard treatment guidelines."

- [ ] **Step 3: Structure the Proof-Density Badge Node**
  - Verify that the operational architectural status string resides directly underneath the main header block within a distinct capsule wrapper (`<div class="status-pill">`).
  - Set its internal string content to: "Offline-First Fallback Engine Active · Zero-Hallucination Bound Enforcement".

- [ ] **Step 4: Commit DOM Header Formatting Updates**
  - Run git verification commands to track changes exactly:
    ```bash
    git add frontend/index.html
    git commit -m "style: format landing hero messaging layout with mechanism-first psychological framing"
    ```

### Task 2: Search Input & Voice of Customer Preset Integration (`index.html`)

**Files:**
- Modify: `frontend/index.html:86-130`

- [ ] **Step 1: Structure the Form Input Substrate**
  - Format the interactive text input tag (`<input id="search-input">`) to enforce a minimum line height (`line-height: 1.5`) and full mobile accessibility sizing (`min-height: 54px`).
  - Append descriptive placeholder attributes targeting immediate high-intent practitioner testing: `placeholder="Interrogate clinical standard treatment bounds, herb-drug contraindications, or classical operative measures..."`.

- [ ] **Step 2: Format the Tactile VoC Chip Anchors**
  - Wrap the dynamic trigger suggestion blocks within a designated horizontal scrolling container (`<div class="suggestions-wrapper" role="region" aria-label="Interrogation Presets">`).
  - Assign distinct click-tracking target classes (`class="chip-btn"`) to each individual query node.

- [ ] **Step 3: Map Specific High-Agency Query Targets**
  - Explicitly inject three targeted clinical interrogations into the chip structures to guide conversion paths:
    1. First Chip String: "Ministry of AYUSH Essential Hypertension Treatment Protocol"
    2. Second Chip String: "Sushruta Samhita Shashti Upakrama Operative Matrix"
    3. Third Chip String: "Pippali Yogavahi Bio-Availability Enhancement Pathway"

- [ ] **Step 4: Commit Voice of Customer Preset Formatting**
  - Track modified layout markup via terminal execution:
    ```bash
    git add frontend/index.html
    git commit -m "style: integrate actionable voice of customer interrogation preset buttons"
    ```

### Task 3: Visual Output Framing & Contrast Tokens (`styles.css`)

**Files:**
- Modify: `frontend/styles.css:120-195`

- [ ] **Step 1: Establish High-Contrast Viewport Substrates**
  - Assign the absolute viewport backplane variables to ensure reading focus remains unchallenged: set `--bg-dark` to `#090D16` and define `--bg-surface` as `rgba(15, 23, 42, 0.75)`.

- [ ] **Step 2: Configure Layout Padding Rhythms**
  - Enforce continuous reading comfort by applying structured margin spacing properties across main child elements: `.result-card` container must enforce uniform interior padding (`padding: 2.5rem`).

- [ ] **Step 3: Define Distinct Structural Border Variables**
  - Configure the left structural card boundary token (`--border-glass`) using a warm, desaturated gold refraction ratio (`rgba(217, 119, 6, 0.25)`) to visually distinguish formal answers from background elements.

- [ ] **Step 4: Commit Structural CSS Variables**
  - Save style token definitions to workspace version control:
    ```bash
    git add frontend/styles.css
    git commit -m "style: establish premium background variables and container layout spacing rhythms"
    ```

### Task 4: Interactive Tooltip Hover Overlay Architecture (`styles.css`)

**Files:**
- Modify: `frontend/styles.css:380-450`

- [ ] **Step 1: Implement Inline Keyword Interaction Styling**
  - Configure the text highlight anchor class (`.entity-highlight`) to enforce clear user affordance: assign standard gold text highlighting (`color: var(--accent-gold)`), a thin dashed bottom edge (`border-bottom: 1px dashed var(--accent-gold)`), and an explicit cursor mapping (`cursor: help`).

- [ ] **Step 2: Map the Custom Overlay Bubble Properties**
  - Format the detached pop-up container (`.custom-tooltip`) to ensure high visual layering: set its positioning context (`position: absolute`), assign an ultra-dense backdrop filter (`backdrop-filter: blur(12px)`), and limit extreme expansions (`max-width: 320px`).

- [ ] **Step 3: Implement Smooth Motion Transition Curves**
  - Assign initial offset states to the bubble layer (`transform: translateY(8px); opacity: 0; pointer-events: none;`).
  - Define the activated transition timeline (`transition: opacity 0.15s ease, transform 0.15s ease;`) to achieve highly responsive physical feedback upon cursor entry.

- [ ] **Step 4: Commit Tooltip Styling Properties**
  - Stage and record custom pop-up layout additions:
    ```bash
    git add frontend/styles.css
    git commit -m "style: implement custom interactive floating overlay tooltip styling properties"
    ```

### Task 5: Self-Review & Formatting Integrity Check

**Files:**
- Review: `frontend/index.html`
- Review: `frontend/styles.css`

- [ ] **Step 1: Verify DOM Hierarchy Anchoring**
  - Cross-reference all interactive class hooks (`.entity-highlight`, `.chip-btn`, `#custom-tooltip`) to ensure matching structural identifiers are present across formatting scripts.

- [ ] **Step 2: Execute Placeholder Scan Check**
  - Scan both output files to confirm zero temporary strings ("TODO", "TBD", or unmapped properties) are active within primary layout nodes.

- [ ] **Step 3: Commit Final Validation State**
  - Finalize formatting verification inside the repository environment:
    ```bash
    git commit --allow-empty -m "chore: verify layout and formatting alignment against psychological conversion requirements"
    ```
